<div class="container">
  <div class="row">
    <h1>All our Flats (<%= Flat.count %>)</h1>
  </div>

    <div class="row">
      <% @flats.each do |flat| %>
        <div class="col-4">
          <div class="flat-card">
            <img class="flat-card-image" src="https://source.unsplash.com/random/1150x600" alt="Card image cap">
            <div class="flat-card-content">
              <h5 class="flat-card-title"><%= flat.name %></h5>
              <p class="card-text"><%= flat.address %></p>
              <div class="flex">
                <div class="flex"><p><%= flat.number_of_guests %></p></div>
                <div class="flex ml-3"><p>£<%= flat.price_per_night %></p></div>
              </div>
            </div>
            <div class="flat-card-actions">
              <a href="#" class="">Card link</a>
              <a href="#" class="">Another link</a>
            </div>
          </div>
        </div>
      <% end %>
    </div>

</div>


